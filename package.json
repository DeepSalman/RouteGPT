{
  "name": "routegpt",
  "version": "0.1.0",
  "private": true,
  "description": "RouteGPT: a conversational AI transport assistant for Dhaka.",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev --workspace backend\" \"npm run dev --workspace frontend\"",
    "dev:backend": "npm run dev --workspace backend",
    "dev:frontend": "npm run dev --workspace frontend",
    "scrape:bus": "npm --workspace backend run scrape --",
    "db:schema": "npm --workspace backend run db:schema",
    "db:seed": "npm --workspace backend run db:seed --",
    "build": "npm run build --workspace frontend",
    "start": "npm run start --workspace backend",
    "test": "npm --workspace backend test && npm --workspace frontend test",
    "install:all": "npm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
